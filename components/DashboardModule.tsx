"use client";
import {useEffect,useMemo,useState} from "react";
import {Activity,AlertTriangle,Bell,CheckCircle2,Droplets,FlaskConical,Gauge,Leaf,ShieldCheck,TestTube2,Thermometer,TimerReset,TriangleAlert,Waves} from "lucide-react";
import {createClient} from "@/lib/supabase/client";
import {measurementStatus,measurementStatusLabel} from "@/lib/measurementStatus";
import type {Aquarium} from "@/components/AquariumsModule";
type Session={aquarium_id:string;measured_at:string;measurement_values:{parameter_code:string;value:number}[]};
type Task={aquarium_id:string;title:string;due_at:string;completed_at?:string|null};
type Observation={aquarium_id:string;issue_code:string;severity:string;status:string};
type Alert={aquarium_id:string;level:'red'|'orange';title:string;detail:string};
type AquariumState={label:string;detail:string;cls:string;dot:string;level:'green'|'orange'|'red'};
const labels:Record<string,string>={ph:'pH',gh:'GH',kh:'KH',no2:'NO₂',nh3:'NH₃',nh4:'NH₄',no3:'NO₃',po4:'PO₄',fe:'Fe',k:'K',mg:'Mg',tds:'TDS'};
const units:Record<string,string>={gh:' °dGH',kh:' °dKH',no2:' mg/l',nh3:' mg/l',nh4:' mg/l',no3:' mg/l',po4:' mg/l',fe:' mg/l',k:' mg/l',mg:' mg/l',tds:' ppm'};
const issueLabels:Record<string,string>={bba:'Čierna štetinková riasa BBA',cyanobacteria:'Sinice BGA',cladophora:'Cladophora',brown:'Hnedá riasa',green_dust:'Zelená prachová riasa GDA',green_spot:'Zelená fľekovitá riasa GSA',green_water:'Zelená voda',hair:'Vláknitá riasa',oedogonium:'Oedogonium',rhizoclonium:'Rhizoclonium',spirogyra:'Spirogyry',staghorn:'Staghorn',other:'Iný problém'};
const statusPriority=['no2','nh3','nh4','ph','no3','po4','gh','kh','temperature','o2','tds','ec','fe','k','mg','ca'];
export default function DashboardModule({aquariums}:{aquariums:Aquarium[]}){
 const[sessions,setSessions]=useState<Session[]>([]);const[tasks,setTasks]=useState<Task[]>([]);const[obs,setObs]=useState<Observation[]>([]);const[loading,setLoading]=useState(true);
 useEffect(()=>{(async()=>{if(!aquariums.length){setLoading(false);return}setLoading(true);const s=createClient();const ids=aquariums.map(a=>a.id);const[m,o]=await Promise.all([s.from('measurement_sessions').select('aquarium_id,measured_at,measurement_values(parameter_code,value)').in('aquarium_id',ids).order('measured_at',{ascending:false}).limit(60),s.from('aquarium_observations').select('aquarium_id,issue_code,severity,status').in('aquarium_id',ids).neq('status','resolved')]);if(!m.error)setSessions((m.data||[]) as any);if(!o.error)setObs((o.data||[]) as any);const t=await s.from('aquarium_tasks').select('aquarium_id,title,due_at,completed_at').in('aquarium_id',ids).is('completed_at',null).order('due_at',{ascending:true}).limit(20);if(!t.error)setTasks((t.data||[]) as any);setLoading(false)})()},[aquariums]);
 const aquariumNames=useMemo(()=>Object.fromEntries(aquariums.map(a=>[a.id,a.name])),[aquariums]);
 const latest=useMemo(()=>{const map=new Map<string,Session>();for(const s of sessions)if(!map.has(s.aquarium_id))map.set(s.aquarium_id,s);return map},[sessions]);
 const waterAlerts=useMemo(()=>{const out:Alert[]=[];for(const a of aquariums){const s=latest.get(a.id);if(!s)continue;const v=Object.fromEntries((s.measurement_values||[]).map(x=>[x.parameter_code,Number(x.value)])) as Record<string,number>;let internalScore=0;const why:string[]=[];if((v.no2||0)>0){internalScore+=45;why.push(`NO₂ ${v.no2} mg/l`)}if((v.nh3||0)>0||(v.nh4||0)>0){internalScore+=45;why.push('NH₃/NH₄ je nad nulou')}if((v.no3||0)>=30){internalScore+=25;why.push(`NO₃ ${v.no3} mg/l`)}if((v.po4||0)>=2){internalScore+=12;why.push(`PO₄ ${v.po4} mg/l`)}if(internalScore>=70)out.push({aquarium_id:a.id,level:'red',title:'Urgentná výmena vody',detail:`Odporúčaná výmena približne 45 %. ${why.join(' · ')}`});else if(internalScore>=45)out.push({aquarium_id:a.id,level:'orange',title:'Voda vyžaduje pozornosť',detail:`Odporúčaná výmena približne 35 %. ${why.join(' · ')}`});else if(internalScore>=25)out.push({aquarium_id:a.id,level:'orange',title:'Zváž výmenu vody',detail:`Odporúčaná výmena približne 25 %. ${why.join(' · ')}`})}return out},[aquariums,latest]);
 const observationAlerts=useMemo(()=>obs.map(o=>({aquarium_id:o.aquarium_id,level:(o.severity==='strong'?'red':'orange') as 'red'|'orange',title:issueLabels[o.issue_code]||o.issue_code,detail:o.status==='worsening'?'Výskyt sa zhoršuje.':'Aktívne pozorovanie v údržbe.'})),[obs]);
 const alerts=[...waterAlerts,...observationAlerts];const overdue=tasks.filter(t=>new Date(t.due_at).getTime()<Date.now());
 function stateFor(id:string):AquariumState{
  const ownAlerts=alerts.filter(a=>a.aquarium_id===id);const s=latest.get(id);
  if(!s)return{label:'Vyžaduje pozornosť',detail:'Chýba prvé meranie vody',cls:'status-orange',dot:'🟠',level:'orange'};
  const values=(s.measurement_values||[]).map(v=>({...v,status:measurementStatus(v.parameter_code,Number(v.value))}));
  const sorted=values.slice().sort((a,b)=>statusPriority.indexOf(a.parameter_code)-statusPriority.indexOf(b.parameter_code));
  const bad=sorted.find(v=>v.status==='bad');
  if(bad){const name=labels[bad.parameter_code]||bad.parameter_code.toUpperCase();return{label:`${name} mimo rozsahu`,detail:`${name} ${Number(bad.value)}${units[bad.parameter_code]||''}`,cls:'status-red',dot:'🔴',level:'red'}}
  if(ownAlerts.some(a=>a.level==='red')){const a=ownAlerts.find(a=>a.level==='red')!;return{label:'Vyžaduje zásah',detail:a.title,cls:'status-red',dot:'🔴',level:'red'}}
  const warning=sorted.find(v=>v.status==='warning');
  if(warning){const name=labels[warning.parameter_code]||warning.parameter_code.toUpperCase();return{label:'Vyžaduje pozornosť',detail:`${name} je na hranici odporúčaného rozsahu`,cls:'status-orange',dot:'🟠',level:'orange'}}
  if(ownAlerts.length){return{label:'Vyžaduje pozornosť',detail:ownAlerts[0].title,cls:'status-orange',dot:'🟠',level:'orange'}}
  return{label:'V poriadku',detail:'Posledné dostupné údaje sú v odporúčanom rozsahu',cls:'status-green',dot:'🟢',level:'green'};
 }
 const states=useMemo(()=>Object.fromEntries(aquariums.map(a=>[a.id,stateFor(a.id)])),[aquariums,latest,alerts]);
 const urgent=Object.values(states).filter(s=>s.level==='red').length;const healthy=Object.values(states).filter(s=>s.level==='green').length;
 if(!aquariums.length)return <section className="v1d-empty"><Waves/><h2>Pridaj svoje prvé akvárium</h2><p>Po vytvorení nádrže tu uvidíš aktuálny stav, merania, úlohy a upozornenia.</p></section>;
 const focus=aquariums[0];const focusState=states[focus.id];const focusSession=latest.get(focus.id);const focusValues=Object.fromEntries((focusSession?.measurement_values||[]).map(v=>[v.parameter_code,Number(v.value)])) as Record<string,number>;
 const focusAlerts=alerts.filter(a=>a.aquarium_id===focus.id).slice(0,4);const focusTasks=tasks.filter(t=>t.aquarium_id===focus.id).slice(0,3);const focusObs=obs.filter(o=>o.aquarium_id===focus.id);
 const priorityParams=(focusSession?.measurement_values||[]).map(v=>({...v,status:measurementStatus(v.parameter_code,Number(v.value))})).filter(v=>v.status!=='good').sort((a,b)=>statusPriority.indexOf(a.parameter_code)-statusPriority.indexOf(b.parameter_code)).slice(0,3);
 return <div className="v1d">
  <div className="v1d-heading"><span>PREHĽAD</span><h2>Dobré ráno 👋</h2><p>Najdôležitejšie informácie pre <b>{focus.name}</b> na jednom mieste.</p></div>
  <section className={`v1d-health ${focusState.level}`}><div className="v1d-health-icon">{focusState.level==='green'?<CheckCircle2/>:<AlertTriangle/>}</div><div><span>STAV AKVÁRIA</span><h3>{loading?'Načítavam aktuálny stav…':focusState.label}</h3><p>{focusState.detail}</p></div><div className={`v1d-health-pill ${focusState.level}`}>{focusState.dot} {focusState.level==='red'?'Priorita':focusState.level==='orange'?'Pozornosť':'V poriadku'}</div></section>
  <div className="v1d-cols">
   <section className="v1d-card"><div className="v1d-card-head"><div><span>DNES</span><h3>Čo si pýta pozornosť</h3></div>{priorityParams.length>0&&<b className="v1d-count">{priorityParams.length} {priorityParams.length===1?'odchýlka':'odchýlky'}</b>}</div>
    <div className="v1d-alerts">{priorityParams.length===0&&focusAlerts.length===0?<div className="v1d-good"><CheckCircle2/><div><b>Bez kritických odchýlok</b><small>Podľa posledného dostupného merania netreba mimoriadny zásah.</small></div></div>:<>{priorityParams.map(v=><div className={`v1d-alert ${v.status}`} key={v.parameter_code}><div className="v1d-alert-icon"><FlaskConical/></div><div><b>{labels[v.parameter_code]||v.parameter_code.toUpperCase()} {Number(v.value)}{units[v.parameter_code]||''}</b><small>{measurementStatusLabel(v.status)}</small></div></div>)}{focusAlerts.slice(0,Math.max(0,4-priorityParams.length)).map((a,i)=><div className={`v1d-alert ${a.level==='red'?'bad':'warning'}`} key={`${a.title}-${i}`}><div className="v1d-alert-icon"><TriangleAlert/></div><div><b>{a.title}</b><small>{a.detail}</small></div></div>)}</>}</div>
   </section>
   <section className="v1d-card"><div className="v1d-card-head"><div><span>ODPORÚČANIE</span><h3>Čo teraz</h3></div><ShieldCheck/></div>
    {focusState.level==='red'?<><div className="v1d-rec"><b>1</b><div><strong>Najprv rieš kritickú hodnotu</strong><p>{focusState.detail}. Pred ďalšími korekciami over stav novým meraním.</p></div></div><div className="v1d-rec"><b>2</b><div><strong>Nedávkuj podľa starého merania</strong><p>Pri kritickom stave má stabilita nádrže prednosť pred dorovnávaním ostatných parametrov.</p></div></div></>:focusState.level==='orange'?<><div className="v1d-rec"><b>1</b><div><strong>Skontroluj odchýlku</strong><p>{focusState.detail}. Pri neistote urob kontrolné meranie pred zásahom.</p></div></div><div className="v1d-rec"><b>2</b><div><strong>Sleduj trend</strong><p>Jedna hraničná hodnota nemusí znamenať problém, dôležitý je vývoj v čase.</p></div></div></>:<><div className="v1d-rec"><b>✓</b><div><strong>Pokračuj v bežnom režime</strong><p>Posledné dostupné údaje neukazujú kritickú odchýlku.</p></div></div></>}
   </section>
  </div>
  <section className="v1d-card"><div className="v1d-card-head"><div><span>STAROSTLIVOSŤ</span><h3>Dnešný plán</h3></div><Bell/></div><div className="v1d-tasks">{focusTasks.length===0?<div className="v1d-good"><CheckCircle2/><div><b>Žiadne otvorené úlohy</b><small>Pre toto akvárium momentálne nemáš nič naplánované.</small></div></div>:focusTasks.map((t,i)=>{const late=new Date(t.due_at).getTime()<Date.now();return <div className="v1d-task" key={i}><div className={`v1d-task-icon ${late?'late':''}`}><Bell/></div><div><b>{t.title}</b><small>{late?'Po termíne · ':''}{new Date(t.due_at).toLocaleString('sk-SK')}</small></div></div>})}</div></section>
  <div className="v1d-stats"><div className="v1d-mini"><div><Thermometer/></div><span><small>pH</small><b>{focusValues.ph!=null?focusValues.ph:'—'}</b><em>{focusValues.ph!=null?'posledné meranie':'bez údaja'}</em></span></div><div className="v1d-mini"><div><TimerReset/></div><span><small>Posledné meranie</small><b>{focusSession?new Date(focusSession.measured_at).toLocaleDateString('sk-SK'):'—'}</b><em>{focusSession?new Date(focusSession.measured_at).toLocaleTimeString('sk-SK',{hour:'2-digit',minute:'2-digit'}):'bez záznamu'}</em></span></div><div className="v1d-mini"><div><Leaf/></div><span><small>Aktívne pozorovania</small><b>{focusObs.length}</b><em>{focusObs.length?'vyžadujú sledovanie':'bez aktívneho problému'}</em></span></div></div>
  {aquariums.length>1&&<section className="v1d-card v1d-all"><div className="v1d-card-head"><div><span>VŠETKY AKVÁRIÁ</span><h3>Aktuálny stav nádrží</h3></div><Gauge/></div><div className="v1d-aquariums">{aquariums.map(a=>{const st=states[a.id];return <div key={a.id}><span><b>{a.name}</b><small>{a.net_volume_l} l</small></span><em className={st.level}>{st.dot} {st.label}</em></div>})}</div></section>}
  <section className="v1d-card"><div className="v1d-card-head"><div><span>HISTÓRIA</span><h3>Posledné meranie</h3></div><Activity/></div>{!focusSession?<p className="v1d-muted">Zatiaľ bez merania.</p>:<div className="v1d-measures">{(focusSession.measurement_values||[]).filter(v=>labels[v.parameter_code]).slice(0,8).map(v=>{const status=measurementStatus(v.parameter_code,Number(v.value));return <div className={status} key={v.parameter_code}><Droplets/><span><small>{labels[v.parameter_code]}</small><b>{Number(v.value)}{units[v.parameter_code]||''}</b></span></div>})}</div>}</section>
  <style>{css}</style>
 </div>;
}
const css=`
.v1d{--v1:#0f7d68;--ink:#17332f;--muted:#71847f;--line:#dfe9e6;color:var(--ink)}
.v1d-heading{margin:4px 0 22px}.v1d-heading>span,.v1d-card-head span,.v1d-health span{font-size:9px;font-weight:850;letter-spacing:.13em;color:#82938f}.v1d-heading h2{font-size:29px;margin:5px 0 6px;letter-spacing:-.035em}.v1d-heading p{font-size:13px;color:var(--muted);margin:0}.v1d-heading p b{color:#425d57}
.v1d-health{display:grid;grid-template-columns:48px 1fr auto;gap:15px;align-items:center;border-radius:18px;padding:21px;margin-bottom:17px;border:1px solid #dce8e5;background:white;box-shadow:0 2px 12px rgba(18,56,48,.035)}.v1d-health.red{background:linear-gradient(135deg,#fff5ee,#fff);border-color:#efd2ba}.v1d-health.orange{background:linear-gradient(135deg,#fff9ef,#fff);border-color:#eeddb9}.v1d-health.green{background:linear-gradient(135deg,#f2fbf7,#fff);border-color:#cde8dd}.v1d-health-icon{width:48px;height:48px;border-radius:14px;display:grid;place-items:center;color:#fff;background:#ef9b3d}.v1d-health.red .v1d-health-icon{background:#c85a4f}.v1d-health.green .v1d-health-icon{background:#2f9a71}.v1d-health-icon svg{width:24px}.v1d-health h3{font-size:19px;margin:3px 0}.v1d-health p{font-size:11px;color:#786e62;margin:0}.v1d-health-pill{border-radius:999px;padding:7px 10px;font-size:10px;font-weight:850;background:#fff;border:1px solid var(--line);white-space:nowrap}.v1d-health-pill.red{color:#a1423c}.v1d-health-pill.orange{color:#9a651b}.v1d-health-pill.green{color:#287551}
.v1d-cols{display:grid;grid-template-columns:1fr 1fr;gap:17px;margin-bottom:17px}.v1d-card{background:white;border:1px solid var(--line);border-radius:17px;padding:19px;box-shadow:0 2px 11px rgba(16,54,48,.035);margin-bottom:17px}.v1d-card-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;margin-bottom:14px}.v1d-card-head h3{font-size:16px;margin:3px 0 0}.v1d-card-head>svg{width:21px;color:#668985}.v1d-count{font-size:9px;background:#fdecea;color:#a34a43;padding:6px 8px;border-radius:999px}
.v1d-alerts{display:grid;gap:8px}.v1d-alert{display:grid;grid-template-columns:36px 1fr;gap:10px;align-items:center;border:1px solid #e6ecea;border-radius:12px;padding:10px}.v1d-alert-icon{width:36px;height:36px;border-radius:10px;display:grid;place-items:center;background:#f2f6f5;color:#637a75}.v1d-alert-icon svg{width:16px}.v1d-alert.bad .v1d-alert-icon{background:#fdecea;color:#ad4a43}.v1d-alert.warning .v1d-alert-icon{background:#fff3df;color:#9d671d}.v1d-alert b,.v1d-alert small{display:block}.v1d-alert b{font-size:11px}.v1d-alert small{font-size:9px;color:#7e908b;margin-top:2px}
.v1d-rec{display:grid;grid-template-columns:27px 1fr;gap:10px;padding:10px 0;border-top:1px solid #edf2f0}.v1d-rec:first-of-type{border-top:0}.v1d-rec>b{width:27px;height:27px;border-radius:9px;background:#eaf7f3;color:var(--v1);display:grid;place-items:center;font-size:11px}.v1d-rec strong{font-size:11px}.v1d-rec p{font-size:9px;color:var(--muted);line-height:1.5;margin:3px 0 0}
.v1d-tasks{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.v1d-task{border:1px solid #e5ece9;border-radius:12px;padding:11px;display:flex;align-items:center;gap:9px}.v1d-task-icon{width:34px;height:34px;border-radius:10px;background:#edf7f4;color:var(--v1);display:grid;place-items:center}.v1d-task-icon.late{background:#fdeceb;color:#ae4a43}.v1d-task-icon svg{width:15px}.v1d-task b,.v1d-task small{display:block}.v1d-task b{font-size:10px}.v1d-task small{font-size:8px;color:var(--muted);margin-top:3px}
.v1d-good{display:flex;gap:9px;align-items:flex-start;padding:8px 0}.v1d-good>svg{width:17px;color:#2d946d}.v1d-good b,.v1d-good small{display:block}.v1d-good b{font-size:11px}.v1d-good small{font-size:9px;color:var(--muted);margin-top:3px}
.v1d-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:17px}.v1d-mini{background:white;border:1px solid var(--line);border-radius:14px;padding:14px;display:flex;align-items:center;gap:10px}.v1d-mini>div{width:37px;height:37px;border-radius:10px;background:#edf9f5;color:#18886f;display:grid;place-items:center}.v1d-mini>div svg{width:17px}.v1d-mini span,.v1d-mini small,.v1d-mini b,.v1d-mini em{display:block}.v1d-mini small{font-size:8px;color:var(--muted)}.v1d-mini b{font-size:13px;margin:1px 0}.v1d-mini em{font-size:8px;color:#879894;font-style:normal}
.v1d-aquariums{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.v1d-aquariums>div{display:flex;justify-content:space-between;align-items:center;gap:12px;border:1px solid #e5ece9;border-radius:11px;padding:11px}.v1d-aquariums span b,.v1d-aquariums span small{display:block}.v1d-aquariums span b{font-size:10px}.v1d-aquariums span small{font-size:8px;color:var(--muted)}.v1d-aquariums em{font-size:9px;font-style:normal;font-weight:750}.v1d-aquariums em.red{color:#a94841}.v1d-aquariums em.orange{color:#96631d}.v1d-aquariums em.green{color:#287551}
.v1d-measures{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.v1d-measures>div{border:1px solid #e6ecea;border-radius:11px;padding:9px;display:flex;gap:7px;align-items:center;background:#fafcfc}.v1d-measures>div.bad{background:#fff3f1;border-color:#f0d5d1}.v1d-measures>div.warning{background:#fff9ef;border-color:#eedfbe}.v1d-measures>div svg{width:13px;color:#76908a}.v1d-measures span small,.v1d-measures span b{display:block}.v1d-measures span small{font-size:8px;color:var(--muted)}.v1d-measures span b{font-size:10px}.v1d-muted{font-size:10px;color:var(--muted)}
.v1d-empty{background:white;border:1px solid #dfe9e6;border-radius:18px;padding:35px;text-align:center}.v1d-empty>svg{width:38px;color:#18886f}.v1d-empty h2{margin:9px 0 4px}.v1d-empty p{color:#71847f}
@media(max-width:900px){.v1d-cols{grid-template-columns:1fr}.v1d-tasks{grid-template-columns:1fr}.v1d-stats{grid-template-columns:1fr 1fr}.v1d-measures{grid-template-columns:1fr 1fr}.v1d-aquariums{grid-template-columns:1fr}}
@media(max-width:560px){.v1d-heading h2{font-size:24px}.v1d-health{grid-template-columns:45px 1fr;align-items:start}.v1d-health-pill{grid-column:1/-1;width:max-content}.v1d-stats{grid-template-columns:1fr}.v1d-measures{grid-template-columns:1fr}}
`;
