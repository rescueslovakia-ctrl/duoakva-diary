"use client";
import {useEffect,useState} from "react";
import {Home,Waves,FlaskConical,Wrench,Leaf,Fish,Droplets,NotebookPen,Bell,Settings} from "lucide-react";
import {createClient,isSupabaseConfigured} from "@/lib/supabase/client";
const nav=[["Prehľad",Home],["Akváriá",Waves],["Merania",FlaskConical],["Technika",Wrench],["Rastliny",Leaf],["Osádka",Fish],["Hnojenie",Droplets],["Údržba",NotebookPen],["Úlohy",Bell],["Nastavenia",Settings]] as const;
export default function App(){const[page,setPage]=useState("Prehľad");const[email,setEmail]=useState("");useEffect(()=>{if(!isSupabaseConfigured())return;createClient().auth.getUser().then(({data})=>setEmail(data.user?.email||""))},[]);return <div className="app"><aside><div className="logo">DuoAkva <b>Diary</b></div>{nav.map(([n,I])=><button className={page===n?"on":""} key={n} onClick={()=>setPage(n)}><I size={18}/>{n}</button>)}</aside><main><header><div><small>DUOAKVA DIARY</small><h1>{page}</h1></div>{email?<span className="account">{email}</span>:<a className="primary link" href="/auth">Prihlásiť sa</a>}</header>{page==="Prehľad"?<><section className="hero"><div><small>INTELIGENTNÝ AKVARISTICKÝ DENNÍK</small><h2>Všetko o akváriu na jednom mieste</h2><p>Parametre vody, údržba, technika, rastliny, osádka, hnojenie a inteligentné odporúčania.</p></div><div className="score">DuoAkva</div></section><div className="grid3"><Card title="Parametre vody"><p>pH · GH · KH · NO₂ · NO₃ · PO₄ · Fe · K · Mg · TDS</p><p className="muted">História meraní a cieľové rozsahy podľa typu nádrže.</p></Card><Card title="Inteligentná údržba"><p>Výmeny vody, filter, hadice, CO₂, kalibrácie a pravidelné kontroly.</p></Card><Card title="Odporúčania"><p>Svetlo, filtrácia, rastliny, osádka a dávkovanie dostupných hnojív.</p></Card></div></>:<Card title={page}><p>Modul <b>{page}</b> je pripravený na prepojenie s databázou DuoAkva Diary.</p><p className="muted">Po nasadení Supabase schémy sem pripojíme používateľské dáta a odporúčací engine.</p></Card>}</main></div>}
function Card({title,children}:{title:string,children:React.ReactNode}){return <section className="card"><h3>{title}</h3>{children}</section>}
