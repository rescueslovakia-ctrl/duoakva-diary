"use client";
import {useState} from "react";
import {createClient,isSupabaseConfigured} from "@/lib/supabase/client";
export default function AuthPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[mode,setMode]=useState<"login"|"signup">("login");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(!isSupabaseConfigured()){setMsg("Supabase ešte nie je nakonfigurovaný.");return}const s=createClient();const r=mode==="signup"?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});if(r.error){setMsg(r.error.message);return}if(mode==="login")location.href="/";else setMsg("Účet vytvorený. Skontroluj e-mail, ak je zapnuté potvrdenie.")}return <main className="auth"><section className="card"><small>DUOAKVA DIARY</small><h1>{mode==="login"?"Prihlásenie":"Registrácia"}</h1><form onSubmit={submit} className="form one"><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Heslo<input type="password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary">{mode==="login"?"Prihlásiť":"Vytvoriť účet"}</button></form>{msg&&<div className="notice">{msg}</div>}<button onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Nemáš účet? Registrácia":"Už máš účet? Prihlásenie"}</button></section></main>}
