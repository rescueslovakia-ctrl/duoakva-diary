import {NextRequest,NextResponse} from "next/server";
import {createServerSupabaseClient} from "@/lib/supabase/server";
import {isAdminUser} from "@/lib/auth/admin";

export const runtime="nodejs";
export const maxDuration=45;

type Analysis={summary:string;condition:"good"|"watch"|"attention";plant_cover_percent:number|null;algae_detected:boolean;algae_types:string[];algae_severity:"none"|"mild"|"moderate"|"strong"|"unknown";water_clarity:"clear"|"slightly_hazy"|"hazy"|"unknown";glass_cleanliness:"clean"|"minor_deposits"|"dirty"|"unknown";plant_condition:"good"|"mixed"|"poor"|"unknown";observations:string[];suggested_actions:string[];confidence:"low"|"medium"|"high";trend:"improved"|"stable"|"worse"|"unknown";change_summary:string|null;internal_trend_score:number|null};
const measurementLabels:Record<string,string>={ph:"pH",gh:"GH",kh:"KH",no2:"NO₂",no3:"NO₃",nh3:"NH₃",nh4:"NH₄",po4:"PO₄",fe:"Fe",k:"K",mg:"Mg",ca:"Ca",tds:"TDS",ec:"vodivosť",temperature:"teplota",o2:"O₂"};
const measurementUnits:Record<string,string>={gh:"°dGH",kh:"°dKH",no2:"mg/l",no3:"mg/l",nh3:"mg/l",nh4:"mg/l",po4:"mg/l",fe:"mg/l",k:"mg/l",mg:"mg/l",ca:"mg/l",tds:"ppm",ec:"µS/cm",temperature:"°C",o2:"mg/l"};
function clampScore(v:any){const n=Number(v);return Number.isFinite(n)?Math.max(0,Math.min(100,Math.round(n))):null}
function normalize(x:any,hasPrevious:boolean):Analysis{return{summary:String(x?.summary||"Na fotografii nebolo možné spoľahlivo určiť stav akvária."),condition:["good","watch","attention"].includes(x?.condition)?x.condition:"watch",plant_cover_percent:Number.isFinite(Number(x?.plant_cover_percent))?Math.max(0,Math.min(100,Math.round(Number(x.plant_cover_percent)))):null,algae_detected:Boolean(x?.algae_detected),algae_types:Array.isArray(x?.algae_types)?x.algae_types.map(String).slice(0,6):[],algae_severity:["none","mild","moderate","strong","unknown"].includes(x?.algae_severity)?x.algae_severity:"unknown",water_clarity:["clear","slightly_hazy","hazy","unknown"].includes(x?.water_clarity)?x.water_clarity:"unknown",glass_cleanliness:["clean","minor_deposits","dirty","unknown"].includes(x?.glass_cleanliness)?x.glass_cleanliness:"unknown",plant_condition:["good","mixed","poor","unknown"].includes(x?.plant_condition)?x.plant_condition:"unknown",observations:Array.isArray(x?.observations)?x.observations.map(String).slice(0,8):[],suggested_actions:Array.isArray(x?.suggested_actions)?x.suggested_actions.map(String).slice(0,6):[],confidence:["low","medium","high"].includes(x?.confidence)?x.confidence:"low",trend:hasPrevious&&["improved","stable","worse","unknown"].includes(x?.trend)?x.trend:"unknown",change_summary:hasPrevious&&x?.change_summary?String(x.change_summary):null,internal_trend_score:clampScore(x?.internal_trend_score)}}
function dateLabel(v:string|null|undefined){if(!v)return null;const d=new Date(v);return Number.isNaN(d.getTime())?null:new Intl.DateTimeFormat("sk-SK",{day:"numeric",month:"numeric",year:"numeric"}).format(d)}
function dateTimeLabel(v:string|null|undefined){if(!v)return null;const d=new Date(v);return Number.isNaN(d.getTime())?null:new Intl.DateTimeFormat("sk-SK",{day:"numeric",month:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit"}).format(d)}
function daysBetween(a:string|null|undefined,b:string|null|undefined){if(!a||!b)return null;const x=new Date(a),y=new Date(b);if(Number.isNaN(x.getTime())||Number.isNaN(y.getTime()))return null;return Math.max(0,Math.floor((y.getTime()-x.getTime())/(24*60*60*1000)))}
function measurementContext(session:any,photoTakenAt:string,waterChange:any){
 if(!session)return "\nMERANIA: Pred dátumom fotografie nie je uložené žiadne meranie. Chemické hodnoty z fotografie neodhaduj a ak by aktuálne meranie pomohlo posúdiť stav, odporuč používateľovi premerať vodu.";
 const values=(session.measurement_values||[]).filter((v:any)=>Number.isFinite(Number(v.value)));
 if(!values.length)return "\nMERANIA: Najbližšie meranie pred fotografiou neobsahuje použiteľné hodnoty. Ak sú chemické parametre potrebné na posúdenie situácie, odporuč nové meranie.";
 const age=daysBetween(session.measured_at,photoTakenAt);const list=values.map((v:any)=>`${measurementLabels[v.parameter_code]||v.parameter_code}: ${Number(v.value)}${measurementUnits[v.parameter_code]?` ${measurementUnits[v.parameter_code]}`:""}`).join(", ");const note=session.note?` Poznámka k meraniu: ${String(session.note).slice(0,300)}.`:"";
 const changeAfterMeasurement=waterChange&&new Date(waterChange.performed_at).getTime()>new Date(session.measured_at).getTime();
 if(changeAfterMeasurement){const amount=Number(waterChange.water_change_percent)>0?`${Number(waterChange.water_change_percent).toFixed(0)} %`:Number(waterChange.water_change_l)>0?`${Number(waterChange.water_change_l)} l`:'nezadaného objemu';return `\nMERANIA: Posledné meranie bolo ${dateTimeLabel(session.measured_at)} a hodnoty boli ${list}.${note} PO TOMTO MERANÍ však prebehla výmena vody (${dateTimeLabel(waterChange.performed_at)}, ${amount}). Preto tieto namerané hodnoty považuj za NEPLATNÉ pre chemický stav na aktuálnej fotografii, bez ohľadu na to, ako sú staré. Nepoužívaj ich na diagnózu ani na vysvetlenie príčiny. Ak sú chemické parametre pre posúdenie relevantné, odporuč nové meranie po výmene vody.`}
 if(age!==null&&age>7)return `\nMERANIA: Posledné meranie je STARÉ (${dateTimeLabel(session.measured_at)}, približne ${age} dní pred fotografiou). Historické hodnoty boli: ${list}.${note} Tieto hodnoty NEPOUŽÍVAJ na hodnotenie aktuálneho chemického stavu ani na vysvetlenie príčiny viditeľného problému. Môžeš ich spomenúť iba ako historický údaj. Ak by chemické parametre pomohli pri rozhodovaní, medzi odporúčanými krokmi uprednostni nové meranie vody.`;
 if(age!==null&&age>=4)return `\nMERANIA: Posledné meranie bolo ${dateTimeLabel(session.measured_at)}, približne ${age} dní pred fotografiou. Hodnoty: ${list}.${note} Meranie už nemusí presne zodpovedať stavu na fotografii. Použi ho len ako slabý kontext, nevytváraj z neho diagnózu ani príčinnú súvislosť a pri neistote odporuč nové meranie.`;
 return `\nMERANIA: Posledné reálne meranie pred fotografiou bolo ${dateTimeLabel(session.measured_at)}${age!==null?` (približne ${age} dní pred fotografiou)`:""}. Namerané hodnoty: ${list}.${note} Tieto čísla sú údaje z denníka, nie hodnoty odhadnuté z fotografie. Môžeš ich použiť ako aktuálny kontext, ale stále netvrď príčinnú súvislosť bez podpory fotografie a ďalších údajov.`;
}

export async function GET(){try{const supabase=await createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Neprihlásený používateľ."},{status:401});const admin=isAdminUser(user);if(admin)return NextResponse.json({available:true,nextAvailableAt:null,isAdmin:true});const{data}=await supabase.from("photo_ai_usage").select("used_at").eq("user_id",user.id).order("used_at",{ascending:false}).limit(1);const last=data?.[0]?.used_at?new Date(data[0].used_at):null;const next=last?new Date(last.getTime()+7*24*60*60*1000):null;const available=!next||next.getTime()<=Date.now();return NextResponse.json({available,nextAvailableAt:available?null:next?.toISOString()||null,isAdmin:false})}catch{return NextResponse.json({available:false,error:"Stav AI analýzy sa nepodarilo načítať."},{status:500})}}

export async function POST(req:NextRequest){let photoId="";let usageId="";try{const body=await req.json();photoId=String(body?.photoId||"");if(!photoId)return NextResponse.json({error:"Chýba fotografia."},{status:400});const supabase=await createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Neprihlásený používateľ."},{status:401});const admin=isAdminUser(user);const{data:photo,error}=await supabase.from("aquarium_photos").select("id,aquarium_id,image_path,taken_at,note,aquariums!inner(id,name,user_id,setup_date)").eq("id",photoId).single();const aquarium=(photo as any)?.aquariums;if(error||!photo||aquarium?.user_id!==user.id)return NextResponse.json({error:"Fotografia nebola nájdená."},{status:404});let nextAvailableAt:string|null=null;if(!admin){const claim=await supabase.rpc("claim_photo_ai_analysis",{p_photo_id:photoId});if(claim.error)return NextResponse.json({error:"Limit analýzy sa nepodarilo overiť."},{status:500});const c=claim.data?.[0];if(!c?.usage_id)return NextResponse.json({error:"Bezplatná AI analýza je dostupná raz za 7 dní.",nextAvailableAt:c?.next_available_at||null},{status:429});usageId=String(c.usage_id);nextAvailableAt=c?.next_available_at||null}
await supabase.from("aquarium_photos").update({analysis_status:"analyzing"}).eq("id",photoId);
const[currentSigned,measurementResult,waterChangeResult]=await Promise.all([
 supabase.storage.from("aquarium-diary").createSignedUrl(photo.image_path,300),
 supabase.from("measurement_sessions").select("id,measured_at,note,measurement_values(parameter_code,value)").eq("aquarium_id",photo.aquarium_id).lte("measured_at",photo.taken_at).order("measured_at",{ascending:false}).limit(1),
 supabase.from("aquarium_maintenance").select("performed_at,water_change_l,water_change_percent").eq("aquarium_id",photo.aquarium_id).eq("maintenance_type","water_change").lte("performed_at",photo.taken_at).order("performed_at",{ascending:false}).limit(1)
]);
if(!currentSigned.data?.signedUrl)throw new Error("image_unavailable");const measurementSession=measurementResult.error?null:measurementResult.data?.[0]||null;const waterChange=waterChangeResult.error?null:waterChangeResult.data?.[0]||null;const prev=await supabase.from("aquarium_photos").select("id,image_path,taken_at").eq("aquarium_id",photo.aquarium_id).eq("analysis_status","completed").lt("taken_at",photo.taken_at).order("taken_at",{ascending:false}).limit(1);const previousPhoto=prev.data?.[0]||null;let previousUrl:string|null=null;if(previousPhoto){const p=await supabase.storage.from("aquarium-diary").createSignedUrl(previousPhoto.image_path,300);previousUrl=p.data?.signedUrl||null}const apiKey=process.env.OPENAI_API_KEY;if(!apiKey)throw new Error("api_key_missing");const hasPrevious=!!previousUrl;const aquariumAgeDays=daysBetween(aquarium?.setup_date,photo.taken_at);const photoGapDays=previousPhoto?daysBetween(previousPhoto.taken_at,photo.taken_at):null;const ageContext=aquarium?.setup_date&&aquariumAgeDays!==null?`\nKONTEXT NÁDRŽE: Nádrž „${aquarium?.name||"Akvárium"}“ bola založená ${dateLabel(aquarium.setup_date)}. Aktuálna fotografia je z ${dateLabel(photo.taken_at)}; nádrž mala pri fotografovaní približne ${aquariumAgeDays} dní. Pri hodnotení zohľadni vek nádrže a prirodzenú fázu zábehu. V mladej nádrži neoznačuj bežné prechodné prejavy automaticky za vážny problém; zároveň neprehliadni jasné vizuálne známky zhoršenia.`:`\nKONTEXT NÁDRŽE: Dátum založenia nie je zadaný, preto vek nádrže pri hodnotení nepredpokladaj.`;const comparisonContext=hasPrevious&&previousPhoto?` Predchádzajúca analyzovaná fotografia je z ${dateLabel(previousPhoto.taken_at)} a medzi fotografiami je približne ${photoGapDays} dní. Pri hodnotení trendu zohľadni aj tento časový odstup.`:"";const measurements=measurementContext(measurementSession,photo.taken_at,waterChange);
const prompt=`Analyzuj fotografiu celého akvária ako súčasť dlhodobého akvaristického fotodenníka. Prvý obrázok je AKTUÁLNA fotografia.${hasPrevious?" Druhý obrázok je PREDCHÁDZAJÚCA analyzovaná fotografia tej istej nádrže; porovnaj ich a popíš iba vizuálne podložené zmeny.":" Predchádzajúca fotografia nie je k dispozícii."}${ageContext}${comparisonContext}${measurements}\nDÔLEŽITÉ PRAVIDLÁ: Jasne odlišuj (1) čo je priamo VIDITEĽNÉ na fotografii a (2) čo pochádza z NAMERANÝCH ÚDAJOV denníka. Z fotografie neurčuj chemické parametre. Rešpektuj vek merania aj udalosti po meraní. Ak po meraní prebehla výmena vody, predchádzajúce chemické hodnoty sa považujú za neplatné pre aktuálny stav, aj keby išlo o malú výmenu vody. V takom prípade radšej odporuč nové meranie. Čerstvé meranie bez následnej výmeny vody môže byť kontext, 4–7 dní staré iba slabý kontext a meranie staršie ako 7 dní nepoužívaj na hodnotenie aktuálneho chemického stavu. Vek nádrže používaj iba ako kontext.\nVráť VÝHRADNE platný JSON bez markdownu s kľúčmi: summary, condition, plant_cover_percent, algae_detected, algae_types, algae_severity, water_clarity, glass_cleanliness, plant_condition, observations, suggested_actions, confidence, trend, change_summary, internal_trend_score.\ncondition: good|watch|attention. algae_severity: none|mild|moderate|strong|unknown. water_clarity: clear|slightly_hazy|hazy|unknown. glass_cleanliness: clean|minor_deposits|dirty|unknown. plant_condition: good|mixed|poor|unknown. confidence: low|medium|high. trend: improved|stable|worse|unknown. plant_cover_percent 0-100 alebo null. change_summary je krátke porovnanie v čase alebo null. internal_trend_score 0-100 je iba interná vizuálna metrika.\nPri algae_types používaj len vizuálne pravdepodobné typy. Odporúčania majú byť krátke a praktické; neodporúčaj dávkovanie liečiv alebo chémie iba podľa fotografie.`;const content:any[]=[{type:"input_text",text:prompt},{type:"input_image",image_url:currentSigned.data.signedUrl,detail:"low"}];if(previousUrl)content.push({type:"input_image",image_url:previousUrl,detail:"low"});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${apiKey}`},body:JSON.stringify({model:process.env.OPENAI_VISION_MODEL||"gpt-5.4-mini",input:[{role:"user",content}],max_output_tokens:1200})});if(!r.ok)throw new Error(`vision_${r.status}`);const j=await r.json();const text=(j.output||[]).flatMap((o:any)=>o.content||[]).find((x:any)=>x.type==="output_text")?.text||j.output_text||"";let parsed:any;try{parsed=JSON.parse(text)}catch{const m=text.match(/\{[\s\S]*\}/);if(!m)throw new Error("invalid_json");parsed=JSON.parse(m[0])}const analysis=normalize(parsed,hasPrevious);await supabase.from("aquarium_photos").update({analysis_status:"completed",analysis_data:analysis}).eq("id",photoId);return NextResponse.json({analysis,nextAvailableAt,isAdmin:admin})}catch{try{const supabase=await createServerSupabaseClient();if(photoId)await supabase.from("aquarium_photos").update({analysis_status:"failed"}).eq("id",photoId);if(usageId)await supabase.rpc("release_photo_ai_analysis",{p_usage_id:usageId})}catch{}return NextResponse.json({error:"Automatická analýza fotografie sa nepodarila. Kredit nebol spotrebovaný."},{status:500})}}
